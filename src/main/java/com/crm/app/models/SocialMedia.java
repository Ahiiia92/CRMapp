package com.crm.app.models;

import io.swagger.annotations.ApiModel;

@ApiModel(description = "Enum list of the different social Media on which the contact is active on")
public enum SocialMedia {
    LINKEDIN,
    FACEBOOK,
    INSTAGRAM,
    TWITTER
}
